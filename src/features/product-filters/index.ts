/**
 * Product filters feature
 * Exports composable and UI components for filtering products
 */

export { useFilters } from './model/useFilters';
export { default as PriceFilter } from './ui/PriceFilter.vue';
