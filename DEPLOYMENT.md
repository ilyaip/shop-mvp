# Деплой на GitHub Pages

Это руководство поможет вам развернуть проект на GitHub Pages.

## 🚀 Автоматический деплой (рекомендуется)

Проект настроен для автоматического деплоя через GitHub Actions.

### Шаг 1: Создание репозитория на GitHub

1. Перейдите на [GitHub](https://github.com) и создайте новый репозиторий
2. Назовите его, например: `shop-mvp` (или любое другое имя)
3. **Не инициализируйте** репозиторий с README, .gitignore или лицензией

### Шаг 2: Обновление конфигурации

Откройте `vite.config.ts` и обновите `base` URL на название вашего репозитория:

```typescript
base: process.env.NODE_ENV === 'production' ? '/ваш-репозиторий/' : '/',
```

Например, если ваш репозиторий называется `my-shop`:
```typescript
base: process.env.NODE_ENV === 'production' ? '/my-shop/' : '/',
```

### Шаг 3: Инициализация Git и первый коммит

```bash
# Инициализация git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit"

# Добавление remote репозитория (замените на ваш URL)
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git

# Переименование ветки в main (если нужно)
git branch -M main

# Отправка кода на GitHub
git push -u origin main
```

### Шаг 4: Настройка GitHub Pages

1. Перейдите в настройки репозитория на GitHub
2. Откройте раздел **Settings** → **Pages**
3. В разделе **Source** выберите:
   - Source: **GitHub Actions**
4. Сохраните изменения

### Шаг 5: Запуск деплоя

После push в ветку `main`, GitHub Actions автоматически:
1. Установит зависимости
2. Соберет проект
3. Задеплоит на GitHub Pages

Вы можете отслеживать процесс во вкладке **Actions** вашего репозитория.

### Шаг 6: Доступ к сайту

После успешного деплоя ваш сайт будет доступен по адресу:

```
https://ваш-username.github.io/ваш-репозиторий/
```

Например:
```
https://johndoe.github.io/shop-mvp/
```

## 🔄 Обновление сайта

Для обновления сайта просто сделайте commit и push в ветку `main`:

```bash
git add .
git commit -m "Update: описание изменений"
git push
```

GitHub Actions автоматически пересоберет и задеплоит новую версию.

## 🛠 Ручной деплой (альтернатива)

Если вы хотите деплоить вручную:

### Вариант 1: Использование скрипта

1. Обновите `deploy.sh`, заменив `<USERNAME>/<REPO>` на ваши данные:
   ```bash
   git push -f git@github.com:ваш-username/ваш-репозиторий.git main:gh-pages
   ```

2. Запустите скрипт:
   ```bash
   ./deploy.sh
   ```

### Вариант 2: Вручную

```bash
# Сборка проекта
npm run build

# Переход в директорию сборки
cd dist

# Инициализация git
git init
git add -A
git commit -m 'deploy'

# Деплой в ветку gh-pages
git push -f git@github.com:ваш-username/ваш-репозиторий.git main:gh-pages

cd -
```

Затем в настройках GitHub Pages выберите ветку `gh-pages` как источник.

## 🐛 Решение проблем

### Проблема: 404 ошибка после деплоя

**Решение**: Убедитесь, что `base` в `vite.config.ts` соответствует названию репозитория:
```typescript
base: '/название-репозитория/'
```

### Проблема: Стили не загружаются

**Решение**: Проверьте, что все пути в коде относительные и используют алиасы (`@/`).

### Проблема: GitHub Actions не запускается

**Решение**: 
1. Проверьте, что файл `.github/workflows/deploy.yml` существует
2. Убедитесь, что в настройках репозитория включены Actions
3. Проверьте права доступа в Settings → Actions → General

### Проблема: Роутинг не работает (404 на страницах)

**Решение**: GitHub Pages не поддерживает SPA роутинг из коробки. Используйте hash-режим в Vue Router или настройте 404.html для редиректа.

Для hash-режима обновите `src/app/router/index.ts`:
```typescript
import { createRouter, createWebHashHistory } from 'vue-router'

const router = createRouter({
  history: createWebHashHistory(), // Вместо createWebHistory()
  routes: [...]
})
```

## 📝 Полезные команды

```bash
# Локальная разработка
npm run dev

# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview

# Линтинг
npm run lint

# Форматирование
npm run format
```

## 🔗 Полезные ссылки

- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [Vite Deployment Guide](https://vitejs.dev/guide/static-deploy.html)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)

## 💡 Советы

1. **Используйте автоматический деплой** - это проще и надежнее
2. **Проверяйте локально** перед push с помощью `npm run preview`
3. **Следите за размером бандла** - GitHub Pages имеет ограничения
4. **Используйте кэширование** - настроено в GitHub Actions workflow
5. **Мониторьте деплой** во вкладке Actions

---

**Готово!** Ваш интернет-магазин теперь доступен онлайн! 🎉
