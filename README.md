# Vue E-commerce MVP

Современный интернет-магазин на Vue 3 с TypeScript и архитектурой Feature-Sliced Design.

## 🚀 Возможности

- ✅ Каталог товаров с фильтрацией по категориям и цене
- ✅ Детальная страница товара
- ✅ Корзина покупок с сохранением в localStorage
- ✅ Оформление заказа с валидацией формы
- ✅ История заказов в личном кабинете
- ✅ Адаптивный дизайн для всех устройств
- ✅ Оптимизация производительности (lazy loading, code splitting)
- ✅ Доступность (A11y) и семантический HTML
- ✅ Уведомления о действиях пользователя

## 🛠 Технологический стек

- **Frontend Framework**: Vue 3.5+ с Composition API
- **Язык**: TypeScript 5.9+ (strict mode)
- **Сборщик**: Vite 7.2+
- **Управление состоянием**: Pinia 3.0+
- **Маршрутизация**: Vue Router 4.6+
- **Иконки**: lucide-vue-next
- **Линтинг**: ESLint + Prettier
- **Шрифты**: Inter (Google Fonts)

## 📋 Требования

- Node.js 20.19+ или 22.12+
- npm 10+

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск dev-сервера

```bash
npm run dev
```

Приложение будет доступно по адресу: **http://localhost:5173/**

### 3. Сборка для production

```bash
npm run build
```

Собранные файлы будут находиться в директории `dist/`

### 4. Предпросмотр production сборки

```bash
npm run preview
```

## 🌐 Деплой на GitHub Pages

Проект настроен для автоматического деплоя на GitHub Pages через GitHub Actions.

### Быстрый старт (5 минут):

1. **Создайте репозиторий на GitHub** (публичный, без README)

2. **Обновите `vite.config.ts`:**
   ```typescript
   base: process.env.NODE_ENV === 'production' ? '/ваш-репозиторий/' : '/',
   ```

3. **Отправьте код на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/username/repo.git
   git branch -M main
   git push -u origin main
   ```

4. **Включите GitHub Pages:**
   - Settings → Pages → Source: **GitHub Actions**

5. **Готово!** Сайт будет доступен через 2-3 минуты:
   ```
   https://username.github.io/repo/
   ```

### 📚 Подробные инструкции:

- **Быстрый старт:** [QUICK_START.md](./QUICK_START.md)
- **Полная документация:** [DEPLOYMENT.md](./DEPLOYMENT.md)
- **Чеклист:** [GITHUB_PAGES_CHECKLIST.md](./GITHUB_PAGES_CHECKLIST.md)

## 📜 Доступные команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev-сервера с hot reload |
| `npm run build` | Сборка для production |
| `npm run preview` | Предпросмотр production сборки |
| `npm run lint` | Проверка и исправление кода с ESLint |
| `npm run format` | Форматирование кода с Prettier |

## 📁 Структура проекта

Проект организован по методологии **Feature-Sliced Design (FSD)**:

```
src/
├── app/                    # Слой приложения
│   ├── App.vue            # Корневой компонент
│   ├── router/            # Конфигурация маршрутизации
│   ├── stores/            # Глобальные Pinia stores
│   │   ├── cart.ts        # Управление корзиной
│   │   ├── products.ts    # Управление товарами
│   │   ├── user.ts        # Управление пользователем
│   │   └── ui.ts          # Управление UI (уведомления)
│   └── styles/            # Глобальные стили
│       ├── variables.css  # CSS-переменные
│       ├── base.css       # Базовые стили
│       └── index.css      # Главный файл стилей
│
├── pages/                 # Слой страниц (роуты)
│   ├── catalog/           # Страница каталога
│   ├── product/           # Страница товара
│   ├── cart/              # Страница корзины
│   ├── checkout/          # Страница оформления заказа
│   └── profile/           # Страница профиля
│
├── widgets/               # Слой виджетов (композиция features)
│   ├── header/            # Шапка сайта
│   ├── footer/            # Подвал сайта
│   └── cart-widget/       # Виджет корзины в шапке
│
├── features/              # Слой фич (бизнес-логика)
│   ├── add-to-cart/       # Добавление товара в корзину
│   ├── product-filters/   # Фильтрация товаров
│   └── checkout-form/     # Форма оформления заказа
│
├── entities/              # Слой сущностей (бизнес-модели)
│   ├── product/           # Сущность товара
│   │   ├── model/         # Типы и интерфейсы
│   │   └── ui/            # UI-компоненты товара
│   ├── cart/              # Сущность корзины
│   ├── order/             # Сущность заказа
│   └── user/              # Сущность пользователя
│
└── shared/                # Слой общих модулей
    ├── ui/                # UI-kit компоненты
    │   ├── Button.vue
    │   ├── Input.vue
    │   ├── Card.vue
    │   ├── Modal.vue
    │   ├── Skeleton.vue
    │   └── Notification.vue
    ├── lib/               # Утилиты
    │   ├── storage.ts     # Работа с localStorage
    │   ├── validators.ts  # Валидаторы форм
    │   └── formatters.ts  # Форматтеры (цена, дата)
    ├── api/               # API и моковые данные
    │   └── mock-data.ts
    └── types/             # Глобальные типы
        └── global.ts
```

## 🗺 Маршруты приложения

| Путь | Описание |
|------|----------|
| `/` | Редирект на `/catalog` |
| `/catalog` | Каталог товаров с фильтрацией |
| `/product/:id` | Детальная страница товара |
| `/cart` | Корзина покупок |
| `/checkout` | Оформление заказа |
| `/profile` | Личный кабинет с историей заказов |
| `/*` (любой другой) | Редирект на `/catalog` |

## 🎨 Дизайн-система

Проект использует единую дизайн-систему с CSS-переменными:

- **Цвета**: Primary, Secondary, Success, Error, Warning, Info
- **Типографика**: Шрифт Inter с размерами от 12px до 32px
- **Отступы**: Система spacing от 4px до 64px
- **Тени**: 3 уровня теней для создания глубины
- **Радиусы**: Единые border-radius для всех компонентов
- **Анимации**: Плавные transitions для всех интерактивных элементов

## 💾 Хранение данных

Приложение использует **localStorage** для персистентности данных:

- **Корзина**: Автоматическое сохранение при каждом изменении
- **Заказы**: Сохранение истории заказов пользователя

Данные сохраняются в формате JSON и автоматически загружаются при инициализации stores.

## 🎯 Основные компоненты

### Stores (Pinia)

- **useProductsStore**: Управление товарами и фильтрацией
- **useCartStore**: Управление корзиной (добавление, удаление, обновление)
- **useUserStore**: Управление заказами пользователя
- **useUIStore**: Управление уведомлениями

### UI-компоненты

- **Button**: Кнопка с вариантами (primary, secondary, outline)
- **Input**: Поле ввода с валидацией и отображением ошибок
- **Card**: Карточка для контента
- **Modal**: Модальное окно с overlay
- **Skeleton**: Скелетон для состояний загрузки
- **Notification**: Уведомления (success, error, info)

## ⚡️ Оптимизация производительности

- **Lazy Loading**: Все страницы загружаются по требованию
- **Code Splitting**: Разделение кода на chunks для оптимальной загрузки
- **Image Optimization**: Ленивая загрузка изображений
- **v-memo**: Оптимизация рендеринга списков
- **Tree Shaking**: Автоматическое удаление неиспользуемого кода
- **Minification**: Минификация CSS и JS в production

## ♿️ Доступность (A11y)

- Семантический HTML5
- ARIA-атрибуты для интерактивных элементов
- Клавиатурная навигация
- Индикаторы фокуса
- Контрастность цветов соответствует WCAG 2.1
- role="alert" для ошибок валидации

## 📱 Адаптивный дизайн

Приложение полностью адаптивно и корректно отображается на:

- 📱 Мобильных устройствах (320px+)
- 📱 Планшетах (768px+)
- 💻 Десктопах (1024px+)
- 🖥 Больших экранах (1440px+)

## 🔧 Конфигурация

### Алиасы путей

Проект использует алиасы для удобного импорта:

```typescript
import { Button } from '@/shared/ui'
import { useCartStore } from '@/app/stores/cart'
import { ProductCard } from '@/entities/product'
```

Доступные алиасы:
- `@/` → `src/`
- `@/app` → `src/app/`
- `@/pages` → `src/pages/`
- `@/widgets` → `src/widgets/`
- `@/features` → `src/features/`
- `@/entities` → `src/entities/`
- `@/shared` → `src/shared/`

### TypeScript

Проект использует TypeScript в **strict mode** с полной типизацией всех компонентов и функций.

## 📝 Разработка

### Стиль кода

- **ESLint**: Статический анализ кода
- **Prettier**: Автоматическое форматирование
- **TypeScript**: Строгая типизация

### Рекомендации

1. Следуйте принципам Feature-Sliced Design
2. Используйте Composition API вместо Options API
3. Типизируйте все props, emits и composables
4. Используйте алиасы путей для импортов
5. Добавляйте aria-атрибуты для доступности
6. Оптимизируйте изображения и используйте lazy loading

## 🐛 Известные ограничения

- Используются моковые данные (нет реального API)
- Нет аутентификации пользователей
- Нет реальной оплаты
- История заказов хранится только в localStorage

## 🚀 Планы развития

- [ ] Интеграция с реальным API
- [ ] Аутентификация и авторизация
- [ ] Интеграция платежных систем
- [ ] Поиск по товарам
- [ ] Сортировка товаров
- [ ] Избранное
- [ ] Сравнение товаров
- [ ] Отзывы и рейтинги
- [ ] Админ-панель

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Vue E-commerce MVP - учебный проект, созданный для демонстрации современных подходов к разработке на Vue 3.

---

**Приятной разработки! 🎉**
