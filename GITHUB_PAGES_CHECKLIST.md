# ✅ Чеклист для деплоя на GitHub Pages

Используйте этот чеклист, чтобы убедиться, что всё настроено правильно.

## Перед деплоем

- [ ] **Создан репозиторий на GitHub**
  - Репозиторий публичный
  - Без README, .gitignore, лицензии при создании

- [ ] **Обновлен `vite.config.ts`**
  ```typescript
  base: process.env.NODE_ENV === 'production' ? '/название-репозитория/' : '/',
  ```
  - Название репозитория совпадает с реальным
  - Слэши `/` в начале и конце присутствуют

- [ ] **Проект собирается локально**
  ```bash
  npm run build
  ```
  - Нет ошибок сборки
  - Директория `dist/` создана

- [ ] **Проверен preview**
  ```bash
  npm run preview
  ```
  - Приложение работает корректно
  - Все страницы открываются
  - Изображения загружаются

## Инициализация Git

- [ ] **Git инициализирован**
  ```bash
  git init
  ```

- [ ] **Все файлы добавлены**
  ```bash
  git add .
  ```

- [ ] **Сделан первый коммит**
  ```bash
  git commit -m "Initial commit: Vue E-commerce MVP"
  ```

- [ ] **Добавлен remote**
  ```bash
  git remote add origin https://github.com/username/repo.git
  ```

- [ ] **Код отправлен на GitHub**
  ```bash
  git branch -M main
  git push -u origin main
  ```

## Настройка GitHub Pages

- [ ] **Открыты настройки репозитория**
  - Settings → Pages

- [ ] **Выбран источник**
  - Source: **GitHub Actions**

- [ ] **Workflow запущен**
  - Вкладка Actions показывает запущенный workflow
  - Статус: 🟡 In progress или ✅ Success

## Проверка деплоя

- [ ] **Workflow завершился успешно**
  - Зеленая галочка ✅ в Actions
  - Нет ошибок в логах

- [ ] **Сайт доступен**
  - Открывается по адресу: `https://username.github.io/repo/`
  - Главная страница загружается

- [ ] **Роутинг работает**
  - Переход между страницами работает
  - Прямые ссылки на страницы работают
  - Нет 404 ошибок

- [ ] **Стили применены**
  - CSS загружается
  - Дизайн выглядит правильно
  - Адаптивность работает

- [ ] **Изображения загружаются**
  - Все изображения товаров видны
  - Иконки отображаются
  - Нет broken images

- [ ] **Функционал работает**
  - Добавление в корзину работает
  - Фильтры работают
  - Оформление заказа работает
  - LocalStorage сохраняет данные

## Дополнительно (опционально)

- [ ] **Настроен custom domain**
  - CNAME файл создан
  - DNS настроен
  - HTTPS работает

- [ ] **Добавлен Google Analytics**
  - Tracking ID добавлен
  - События отслеживаются

- [ ] **Настроен robots.txt**
  - Файл создан в `public/`
  - Правила индексации настроены

- [ ] **Добавлен sitemap.xml**
  - Файл создан
  - Все страницы включены

## Решение проблем

### ❌ 404 ошибка на главной странице

**Проверьте:**
- [ ] `base` в `vite.config.ts` совпадает с названием репозитория
- [ ] Репозиторий публичный
- [ ] GitHub Pages включен в настройках

### ❌ Стили не загружаются

**Проверьте:**
- [ ] `base` URL правильный
- [ ] Сборка прошла успешно
- [ ] Нет ошибок в консоли браузера

### ❌ Роутинг не работает (404 на страницах)

**Решение:**
- [ ] Используйте hash-режим в роутере
- [ ] Или убедитесь, что `404.html` и скрипт в `index.html` настроены

### ❌ GitHub Actions не запускается

**Проверьте:**
- [ ] Файл `.github/workflows/deploy.yml` существует
- [ ] Actions включены в Settings → Actions → General
- [ ] Права доступа настроены (Settings → Actions → Workflow permissions)

## Обновление сайта

После внесения изменений:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Сайт обновится автоматически через 2-3 минуты.

---

## 🎉 Готово!

Если все пункты отмечены ✅, ваш сайт успешно задеплоен на GitHub Pages!

**URL вашего сайта:**
```
https://ваш-username.github.io/ваш-репозиторий/
```

**Поделитесь ссылкой с друзьями и коллегами!** 🚀
