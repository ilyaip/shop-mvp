# 🚀 Быстрый старт: Деплой на GitHub Pages

## За 5 минут до публикации!

### 1️⃣ Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Введите название репозитория (например: `shop-mvp`)
3. Оставьте репозиторий **публичным**
4. **НЕ** добавляйте README, .gitignore или лицензию
5. Нажмите **Create repository**

### 2️⃣ Обновите конфигурацию

Откройте файл `vite.config.ts` и измените строку:

```typescript
base: process.env.NODE_ENV === 'production' ? '/shop-mvp/' : '/',
```

Замените `shop-mvp` на **название вашего репозитория**.

### 3️⃣ Отправьте код на GitHub

Выполните команды в терминале (замените `ваш-username` и `ваш-репозиторий`):

```bash
# Инициализация git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Vue E-commerce MVP"

# Добавление remote
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git

# Отправка на GitHub
git branch -M main
git push -u origin main
```

### 4️⃣ Включите GitHub Pages

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** (⚙️)
3. В левом меню выберите **Pages**
4. В разделе **Source** выберите: **GitHub Actions**
5. Готово! Деплой запустится автоматически

### 5️⃣ Дождитесь деплоя

1. Перейдите во вкладку **Actions** вашего репозитория
2. Дождитесь завершения workflow (зеленая галочка ✅)
3. Обычно это занимает 2-3 минуты

### 6️⃣ Откройте ваш сайт! 🎉

Ваш сайт будет доступен по адресу:

```
https://ваш-username.github.io/ваш-репозиторий/
```

Например:
```
https://johndoe.github.io/shop-mvp/
```

---

## 🔄 Как обновить сайт?

Просто сделайте изменения и отправьте на GitHub:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Сайт обновится автоматически через 2-3 минуты!

---

## ❓ Проблемы?

### Сайт показывает 404

**Решение:** Проверьте, что `base` в `vite.config.ts` совпадает с названием репозитория:
```typescript
base: process.env.NODE_ENV === 'production' ? '/точное-название-репозитория/' : '/',
```

### GitHub Actions не запускается

**Решение:** 
1. Убедитесь, что в Settings → Actions → General включены Actions
2. Проверьте, что файл `.github/workflows/deploy.yml` существует

### Роутинг не работает (404 на страницах)

**Решение:** Используйте hash-режим в роутере. Откройте `src/app/router/index.ts` и измените:

```typescript
import { createRouter, createWebHashHistory } from 'vue-router'

const router = createRouter({
  history: createWebHashHistory(), // Вместо createWebHistory()
  routes: [...]
})
```

---

## 📚 Подробная документация

Для более детальной информации см. [DEPLOYMENT.md](./DEPLOYMENT.md)

---

**Удачи с вашим интернет-магазином! 🛍️**
